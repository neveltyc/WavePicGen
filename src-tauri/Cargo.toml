[package]
name = "wavepicgen"
version = "0.1.0"
description = "WavePicGen — manual-quality digital timing-diagram editor"
edition = "2021"
rust-version = "1.77"

[lib]
name = "wavepicgen_lib"
crate-type = ["staticlib", "cdylib", "rlib"]

[build-dependencies]
tauri-build = { version = "2", features = [] }

[dependencies]
tauri = { version = "2", features = [] }
